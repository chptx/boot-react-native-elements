# template for boot-react-native with react-native-elements

```bash
#clone this repository
boot dev

#another terminal
cd app; react-native run-ios    # or 'run-android' after launching android emulator

```
